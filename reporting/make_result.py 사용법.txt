make_result.py 사용법

1. 필요 파이썬 툴을 다음 명령어로 설치한다.
pip install pandas
pip install openpyxl
pip install xlsxwriter

2. 모든 결과 json파일들을 make_result.py 파일이 있는 디렉토리에 저장한다.

3. tmp.xlsx 파일을 make_result.py 파일이 있는 디렉토리에 저장한다.

4. make_result.py 파일을 실행한다
